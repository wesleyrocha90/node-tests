{
    "name": "access",
    "version": "1.0.0",
    "scripts": {
        "prestart": "npm install",
        "start": "http-server -a localhost -p 8888 -c-1 --cors"
    },
    "dependencies": {
        "http-server": "^0.9.0"
    }
}
